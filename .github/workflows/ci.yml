name: CI

on:
  push:
    branches: [master, "pr-*"]
  pull_request:
    branches: [master]

jobs:
  test:
    strategy:
      matrix:
        python-version: ["3.13", "3.14"]
    uses: ./.github/workflows/test.yml
    with:
      python-version: ${{ matrix.python-version }}
